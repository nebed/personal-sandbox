apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.langflowide.langflow.backend.persistentVolume.claimName | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.langflowide.langflow.backend.persistentVolume.size | quote }}
  storageClassName: {{ .Values.langflowide.langflow.backend.persistentVolume.existingStorageClassName | quote }}