apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  dnsNames:
  - '*.${domain}'
  issuerRef:
    name: ${issuer}
    kind: ClusterIssuer
  secretName: ${name}
